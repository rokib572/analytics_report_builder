export async function reconcile(_locationId: string, _date: string) {
  // TODO: implement
  // - count orders in DB for location + date
  // - count orders from Square for location + date
  // - return { squareCount, dbCount, discrepancy }
}
